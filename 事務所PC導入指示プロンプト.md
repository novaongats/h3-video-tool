# 【作業依頼】事務所PCへ「H3かんたん動画メーカー（ローカル版）」を導入してください

あなたはこのPCの作業担当AIです。このファイルの内容だけを見て作業を完結させてください。
依頼者は**非エンジニア**です。専門用語は避け、日本語で分かりやすく報告してください。

---

## 0. 作るもの（ゴール）

このPCの中だけで、AI動画生成（MiniMax H3）ができる環境を作ります。

- **生成エンジン**＝ ComfyUI（このPCにインストール）
- **操作画面** ＝ すでに完成している「H3かんたん動画メーカー」というブラウザツール（GitHubから取得）

現在この会社ではクラウドのGPUをレンタルして使っていますが、**このPCで動かせばレンタル料が0円**になります（電気代のみ）。操作画面は今使っているものと全く同じで、登場人物の登録機能・絵コンテ機能などもそのまま使えます。

---

## 🚫 絶対に守るルール

1. **課金ゼロで完結させること。** 有料API（OpenAI/Anthropic/クラウドGPU等）を呼ぶコードを書かない・実行しない。使うのは無料のオープンソースのみ。
2. **APIキーを一切求めない・設定しない。** この作業にAPIキーは不要です。もし「APIキーを入力」という画面が出ても空欄のまま進めてください。
3. **既存の環境を壊さない。** 既にPythonやGPUドライバが入っている場合、バージョンを勝手に上げない。必要なら依頼者に確認する。
4. **各フェーズが終わるたびに動作確認**してから次に進む。飛ばさない。
5. **判断に迷ったら止めて依頼者に質問する。** 「たぶん大丈夫」で進めない。

---

## フェーズ0：このPCで動くか判定する（最初に必ず実施）

### 実行するコマンド（Windowsの場合／PowerShell）

```powershell
nvidia-smi --query-gpu=name,memory.total --format=csv
Get-CimInstance Win32_ComputerSystem | ForEach-Object { "RAM: {0} GB" -f [math]::Round($_.TotalPhysicalMemory/1GB,1) }
"空き容量: {0} GB" -f [math]::Round((Get-PSDrive C).Free/1GB,1)
python --version
```

### 判定基準

| 項目 | 必須ライン | 推奨 | 判定 |
|---|---|---|---|
| GPU | NVIDIA製・VRAM 12GB以上 | 24GB以上 | VRAM 8GB以下、またはNVIDIA以外なら**中止** |
| メインメモリ | 32GB | 64GB | 16GB以下は動作が極端に遅いので**要相談** |
| 空き容量 | 70GB以上 | 100GB | 足りなければ**中止** |
| Python | 3.10〜3.12 | 3.12 | 未インストールなら公式から導入（無料） |

### 速度の目安（5秒の動画1本あたり）

- VRAM 24GB以上 ＋ RAM 64GB → 約2〜5分（実用的）
- VRAM 16GB ＋ RAM 32GB → 約5〜12分
- VRAM 12GB ＋ RAM 16GB → 約15〜25分（待てるなら可）

### 👉 このフェーズの終わり方

**判定結果を依頼者に報告し、「中止」または「要相談」に該当する場合は作業を止めてください。**
報告フォーマット：
```
GPU: （名前）／VRAM （数字）GB
メインメモリ: （数字）GB
空き容量: （数字）GB
判定: 合格 ／ 要相談 ／ 不合格
予想される生成時間: 5秒の動画で約◯分
```

---

## フェーズ1：ComfyUI（生成エンジン）を入れる

1. 公式の**デスクトップ版またはポータブル版**を入手：https://www.comfy.org/download
   （Windowsならポータブル版が簡単。解凍するだけで動きます）
2. **バージョンは必ず 0.33.0 以上**にすること（MiniMax H3の動作に必須）
3. 一度起動して、ブラウザで `http://localhost:8188` が開くことを確認
4. **ComfyUI Manager**（拡張機能の管理ツール）を導入する

✅ 確認：ComfyUIの画面が表示されたら次へ。

---

## フェーズ2：AIモデル（動画を作る本体）をダウンロード

**合計約55GBあります。**回線状況によっては数時間かかります。

ダウンロード元と手順は、ComfyUI公式の日本語ガイドに記載があります：
https://docs.comfy.org/ja/tutorials/video/minimax/minimax-h3

### 必要なファイルと置き場所（ファイル名は絶対に変更しないこと）

| ファイル名 | 置き場所 | 用途 |
|---|---|---|
| `minimax_h3_fl2va_pruned_int8_convrot.safetensors` | `ComfyUI/models/diffusion_models/` | 文章・画像から動画を作る本体（約19.5GB） |
| `minimax_h3_ref2va_pruned_int8_convrot.safetensors` | `ComfyUI/models/diffusion_models/` | 人物固定・部分編集用の本体（約20GB） |
| `qwen3vl_32b_minimax_h3_nvfp4_awq.safetensors` | `ComfyUI/models/text_encoders/` | 文章を理解する部分（約14.6GB） |
| `minimax_h3_video_vae_fp16.safetensors` | `ComfyUI/models/vae/` | 映像の変換 |
| `minimax_h3_audio_vae_fp32.safetensors` | `ComfyUI/models/vae/` | 音声の変換 |

⚠️ **ファイル名が1文字でも違うと動きません。**ダウンロード後、必ず名前を照合してください。

✅ 確認：5つのファイルが正しいフォルダにあり、サイズが途中で切れていないこと。

---

## フェーズ3：追加ノード（部品）を入れる

操作ツールが使うワークフローには、ComfyUI標準以外の部品が含まれます。
**ComfyUI Manager** の「Install Missing Custom Nodes」機能を使うか、以下の名前で検索して導入してください：

- `ResolutionSelector`（画面サイズの自動計算）
- `ComfyMathExpression`（動画の長さの計算）

導入後、ComfyUIを再起動してください。

✅ 確認：ComfyUIの起動ログにエラーが出ていないこと。

---

## フェーズ4：操作ツール「H3かんたん動画メーカー」を入れる

1. 以下から一式をダウンロード（公開リポジトリ・無料）：
   **https://github.com/novaongats/h3-video-tool**
   （緑の「Code」ボタン →「Download ZIP」で可）
2. 分かりやすい場所に解凍する（例：`C:\H3ツール`）
3. `h3_tool` フォルダの中に **`config.json`** という名前で新しいファイルを作り、以下を書き込む：

```json
{
  "local_mode": true,
  "local_url": "http://localhost:8188",
  "auto_stop": false
}
```

> 📌 `"local_mode": true` が「このPCで生成する」という意味です。この設定のときツールはクラウドGPUに一切接続しないため、**課金は発生しません。APIキーも不要です。**

4. 文字コードは **UTF-8（BOMなし）** で保存すること。
   ⚠️ PowerShellの `Out-File` や メモ帳の既定設定だと読み込みに失敗することがあります。Pythonで書き出すのが確実です。

✅ 確認：`config.json` が `h3_tool` フォルダ内にあり、上記3行が入っていること。

---

## フェーズ5：動作確認（ここまで来たら完成間近）

1. ComfyUI を起動しておく（`http://localhost:8188` が開く状態）
2. ツールのフォルダにある **`H3かんたん動画メーカー.bat`** を実行する
   （もし動かなければ、`h3_tool` フォルダで `python server.py` を実行）
3. ブラウザで **`http://localhost:8787`** を開く
4. 画面左上の状態表示が「**このPCで生成（レンタル料なし）**」になっていることを確認
   - 「⚠️ 生成エンジンが起動していません」と出たら、ComfyUIが起動しているか確認する
5. **テスト生成**：「📝文章から」タブで、長さを最短（4秒）・画質を「お試し（最速）」にして、
   プロンプト欄に以下を入れて「🚀 動画を生成する」を押す

```
明るい部屋の窓辺に置かれた白いマグカップ。湯気がゆっくり立ちのぼる。カメラはゆっくり寄っていく。
```

6. 完成した動画が画面に表示されれば**成功**です（初回はモデル読み込みで時間がかかります）

---

## 完了報告（依頼者へ以下を伝えてください）

```
■ スペック判定
GPU / VRAM:
メインメモリ:
空き容量:

■ 導入結果
ComfyUI バージョン:
モデル5ファイル: 配置済み / 一部失敗（内容）
追加ノード: 導入済み / 失敗（内容）
ツール: http://localhost:8787 で起動する / しない
状態表示: 「このPCで生成（レンタル料なし）」と表示された / されない

■ テスト生成
4秒の動画: 成功 / 失敗（エラー内容）
かかった時間: 約◯分

■ 引っかかった点・依頼者に判断してほしいこと
```

---

## うまくいかないときの対処

| 症状 | 対処 |
|---|---|
| 生成中にメモリ不足で落ちる | ComfyUIを `--lowvram` オプション付きで起動する。それでもダメなら画質を「お試し」、長さを4秒に下げる |
| ツールが「生成エンジンが起動していません」と出る | ComfyUIが起動しているか、`http://localhost:8188` が開くか確認 |
| 「ノードが見つかりません」というエラー | フェーズ3の追加ノードが入っていない。ComfyUI Managerで再確認 |
| 生成が極端に遅い | メインメモリ不足の可能性。他のアプリを終了する。根本対策はメモリ増設（依頼者に報告） |
| ツールの画面が真っ白 | ブラウザで F5（再読み込み）。それでもダメならツールを再起動 |

**エラーが出たら、画面のスクリーンショットとエラー文をそのまま依頼者に見せてください。**
